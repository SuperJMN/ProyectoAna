version: 1

github:
  enabled: true
  owner: SuperJMN
  repo: ProyectoAna
  token:
    from: env
    name: GITHUB_TOKEN
  packages:
    - project: EvaluacionesApp.Android/EvaluacionesApp.Android.csproj
      formats:
        - type: Apk
          arch:
            - arm64
          signing:
            keystore:
              from: env
              name: ANDROID_KEYSTORE_BASE64
              encoding: base64
            storePassword:
              from: env
              name: ANDROID_SIGNING_STORE_PASS
            keyAlias: android
            keyPassword:
              from: env
              name: ANDROID_SIGNING_KEY_PASS

    - project: EvaluacionesApp.Desktop/EvaluacionesApp.Desktop.csproj
      formats:
        - type: Deb
          arch:
            - x64
        - type: ExeSetup
          arch:
            - x64

nuget:
  enabled: false

githubPages:
  enabled: false
